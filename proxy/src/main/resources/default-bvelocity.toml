# bVelocity-specific configuration.
#
# This file is the single home for every setting bVelocity adds or repurposes relative to
# upstream Velocity. velocity.toml stays in lock-step with upstream; anything bVelocity owns
# lives here. If this file is absent on first boot, it is seeded from the bundled defaults.
#
# Reload with the standard Velocity reload mechanism. Changes to compression-threshold /
# compression-level take effect for newly established connections; flush-consolidation and
# compress-bound-headroom apply to newly initialized channel pipelines.

# ──────────────────────────────────────────────────────────────────────────────
# Compression
# ──────────────────────────────────────────────────────────────────────────────
# These two settings were relocated here from velocity.toml's [advanced] block. They are the
# protocol-level Minecraft compression knobs and behave exactly as they did there.

[compression]

# How large a Minecraft packet has to be before we compress it. Setting this to zero will
# compress all packets, and setting it to -1 will disable compression entirely.
compression-threshold = 128

# How much compression should be done. bVelocity accepts 0-12 here:
# - Java fallback supports up to 9.
# - Native libdeflate supports up to 12.
# The default is -1, which uses a proxy-preferred aggressive level tuned for the
# compression-ratio sweet spot on typical Minecraft traffic (the ratio plateaus well
# below the maximum level, while CPU cost keeps climbing):
# - 6 for native libdeflate
# - 6 for the Java fallback
compression-level = -1

# ──────────────────────────────────────────────────────────────────────────────
# Optimization
# ──────────────────────────────────────────────────────────────────────────────
# bVelocity network and compression-path optimizations. These are safe by default; flip
# them off only if you have a specific reason to.

[optimization]

# Consolidate outbound writes into fewer flushes. Minecraft traffic is bursty (a backend
# tick can push dozens of packets to one player at once); without consolidation each packet
# triggers its own flush syscall. Consolidating collapses those into a single flush per
# burst, reducing user/kernel transitions on the write path. Disabling restores the
# per-packet flush behavior of stock Velocity.
flush-consolidation-enabled = true

# Maximum number of pending writes buffered before a flush is issued. Larger values
# consolidate more aggressively (fewer flushes) at the cost of holding writes slightly
# longer. 256 is large enough to absorb a full backend tick burst for one player.
flush-consolidation-threshold = 256

# Headroom, in bytes, added above the uncompressed size when pre-allocating the
# compressed-output buffer. zlib's stored-block worst case collapses to roughly
# input + 11 for the dominant sub-64KiB Minecraft packet range, so a small fixed headroom
# pre-allocates enough that the compressor's grow-loop (which discards a full compression
# pass on insufficient room) almost never triggers. Lower values save a little memory at
# the cost of more grow-loop retries; 0 is not recommended.
compress-bound-headroom = 16

# Collect per-packet compression statistics (packet counts, raw vs. wire bytes, savings)
# for the /bv compression command. Collection is cheap (a few LongAdder writes per packet)
# but not free; operators chasing maximum throughput can disable it to make the encode
# hot path fully zero-overhead. When disabled, /bv compression reports an empty window.
compression-stats-enabled = true

# Number of Netty worker (event-loop) threads handling client and backend connections.
# 0 (the default) uses Netty's default of 2 * availableProcessors(), which is right for most
# deployments. Lower this on heavily oversold VPS hosts (where availableProcessors() is inflated),
# or raise it on large bare-metal hosts if profiling shows event-loop contention. Takes effect on
# the next proxy start; not hot-reloadable.
event-loop-threads = 0

# Number of Netty boss (acceptor) threads. A single acceptor is enough unless you are accepting
# tens of thousands of new connections per second; ignored entirely when SO_REUSEPORT is enabled
# (each worker binds its own listening socket in that mode). Takes effect on the next proxy start.
boss-threads = 1

# Number of threads in the DNS resolver pool that resolves backend server hostnames. Blocking DNS
# lookups run off the Netty event loops here; a pool larger than 1 prevents one slow lookup (e.g. a
# stalled upstream) from serializing every other player's backend connection. 2 is enough for most
# networks; raise it if you run many backends across distinct hostnames and see slow switch-overs.
dns-resolver-threads = 2

# Positive DNS cache TTL, in seconds. A successfully resolved hostname is reused for this long
# before re-querying. 30s balances freshness against DNS load; raise it for stable backends with
# long-lived records, lower it (toward 5) if you rely on short-TTL DNS for backend failover.
dns-cache-ttl-seconds = 30

# Negative DNS cache TTL, in seconds. A failed lookup is remembered for this long so a dead or
# misconfigured backend hostname is not re-queried on every connection attempt (each of which can
# block for the system DNS timeout). 0 disables negative caching. Keep this small: NXDOMAIN/SERVFAIL
# results here are not distinguished, so a transient failure should clear quickly.
dns-negative-cache-ttl-seconds = 3

# ──────────────────────────────────────────────────────────────────────────────
# Brand
# ──────────────────────────────────────────────────────────────────────────────
# Controls the server-brand text shown in the upper-right of the client F3 debug screen. The brand
# is what players see identifying the server they're connected to. By default bVelocity appends the
# proxy name to the backend's brand (e.g. "paper (Velocity)"), matching upstream behavior.

[brand]

# How the brand is presented. Two modes are supported:
# - "default": upstream behavior — append the proxy name to the backend's brand.
# - "custom":  replace the brand entirely with the `custom-brand` string below. The string is
#              parsed through PaperMC MiniMessage, so colors, decorations, gradients, etc. are all
#              supported (https://docs.papermc.io/adventure/minimessage/).
mode = "DEFAULT"

# The brand text shown when mode = "custom". Parsed through MiniMessage. Has no effect in
# "default" mode. Must be non-empty when using "custom" mode.
# Examples:
#   custom-brand = "<gradient:#ff7eb3:#ff758f>My Network</gradient>"
#   custom-brand = "<dark_red><bold>Hardcore</bold></dark_red> <gray>Survival</gray>"
custom-brand = "<gradient:#ff7eb3:#ff758f>bVelocity</gradient>"
